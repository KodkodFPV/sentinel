# sentinel
sentinel is a lightweight cross-platform C++ diagnostics layer for embedded and host targets. It provides guard-style checks, error and warning logging and early-return helpers. Valid for std::error_code, std::expected, booleans, enums, pointers, and platform-specific status types through pluggable backends.
